# .env.schema - the contract for this Next.js app's environment variables.
#
# The quality gate runs scripts/check-env.sh against this file, so .env.example
# can never drift from the variables the app actually reads, and a real .env - if
# present - is checked for every required key before you ship. CI and the
# `doctor --run-gate` gate run the same check.
#
# One variable per line:
#   NAME  required|optional  [pattern=<ERE>]   # what it is
# Everything after `#` is a free-text description. Blank and comment lines are
# ignored. A `pattern=` value is an extended regular expression the value must
# match; it must not itself contain a `#`. Mark a variable `required` once the
# app cannot boot without it - the gate then fails on any real .env that leaves
# it unset.
#
# Every variable starts optional so a freshly scaffolded project stays green.
# Tighten it for your app: promote the ones it needs to `required` (an auth
# secret, a database URL), add `pattern=` guards, and declare new variables here
# as you add them - the gate fails if .env.example and this schema drift.
#
# Prefix rules: NEXT_PUBLIC_* is a build-time value inlined into the client
# bundle and shipped to every visitor - never a secret. Unprefixed variables are
# server-only and never reach the browser.

# --- Build-time / public (NEXT_PUBLIC_* is inlined into the client bundle) ---
NODE_ENV            optional  pattern=^(development|test|production)$      # runtime environment
NEXT_PUBLIC_APP_URL optional                                              # public base URL of the app (safe to expose)

# --- Server-only (never reaches the browser) ---
PORT                optional  pattern=^[0-9]+$                             # TCP port the server listens on (the platform usually sets it)
DATABASE_URL        optional                                              # database connection string (if the app uses one)
DATABASE_POOL_URL   optional                                              # pooled connection string for serverless/edge platforms
AUTH_SECRET         optional                                              # signing secret for auth sessions - read from the environment
AUTH_INVITE_CODE    optional                                              # shared code that gates coach self-registration; unset disables signup
API_KEY             optional                                              # example third-party secret - read from the environment
MEDIA_BASE_URL      optional                                              # base URL under which game chapter files (game_sources.file_path) are served to the player; unset serves the stored path as-is
MEDIA_PROXY_BASE_URL optional                                             # base URL for downscaled tagging proxies mirroring the chapter paths (ADR 0006); the browser player prefers these to cut RAM/CPU; unset falls back to MEDIA_BASE_URL
CLIP_MEDIA_ROOT     optional                                              # directory the clip worker writes clips to (MEDIA_BASE_URL's directory); clips.output_path is relative to it, and so are the chapters unless MEDIA_SOURCE_ROOT is set (worker process only)
CLIP_OUTPUT_DIR     optional                                              # subdirectory of CLIP_MEDIA_ROOT for finished clips; defaults to `clips`
MEDIA_SOURCE_ROOT   optional                                              # read-only directory the workers read chapter originals from (the Drive mount, ADR 0008); game_sources.file_path is relative to it; the clip worker falls back to CLIP_MEDIA_ROOT, the ingest worker requires it (worker processes only)
MEDIA_PROXY_ROOT    optional                                              # directory the ingest worker writes the 720p tagging proxies to, mirroring the chapter paths; the filesystem counterpart of MEDIA_PROXY_BASE_URL (ingest worker only, required there)
INGEST_QUIET_MINUTES optional pattern=^[1-9][0-9]*$                      # how long a new Drive game folder must stay unchanged before it is imported; defaults to 120 (ingest worker only)
INGEST_PROXY_THREADS optional pattern=^[1-9][0-9]*$                      # thread cap for one proxy encode; defaults to 2 (ingest worker only)
TEAM_SHARE_TOKEN    optional                                              # unguessable secret in the team clip share link (/share/team/<token>); a secret, never NEXT_PUBLIC; unset disables the team clip view
INGEST_TOKEN        optional                                              # shared secret authorizing the drop-a-folder ingest endpoint (POST /api/ingest, Bearer token); a secret, never NEXT_PUBLIC; unset disables auto-ingest
