# SwiftPM and Xcode output and per-user state; nothing here is shared.
.build/
.swiftpm/
DerivedData/
xcuserdata/
*.xcuserstate

# Per-machine signing values (mac/Local.xcconfig.example shows the keys).
/Local.xcconfig
