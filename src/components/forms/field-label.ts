/**
 * The one look for a form field's label: caption size, semibold, uppercase
 * with wide tracking, in `--text-secondary`. Shared by `Input`, `Select`,
 * `Textarea` and read-only fields such as the share-link field, so every field
 * label on a page reads the same.
 */
export const FIELD_LABEL_CLASS =
  "text-[length:var(--fs-caption)] [font-weight:var(--fw-semibold)] uppercase tracking-[var(--ls-wide)] text-[color:var(--text-secondary)]";
