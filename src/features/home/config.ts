/**
 * How many of the coach's most recent games the landing page shows as a peek.
 * Kept as a named constant so the number is not buried as a magic slice in the
 * page. Games are newest-first, so this is simply the top slice of that list.
 */
export const RECENT_GAMES_PEEK_LIMIT = 3;
